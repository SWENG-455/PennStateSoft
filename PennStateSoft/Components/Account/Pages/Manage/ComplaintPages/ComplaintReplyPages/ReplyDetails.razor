@inject IDbContextFactory<PennStateSoft.Data.UserComplaints> ComplaintsDbFactory
@inject NavigationManager NavigationManager

@attribute [Authorize]

<PageTitle>Reply Details</PageTitle>

<AuthorizeView>
    <div>
        @if (ComplaintReply is null)
        {
            <p><em>Loading...</em></p>
        }
        else
        {
            <div class="mt-5 m-3">
                <div class="m-3">
                    <label for="username" class="form-label" style="color:#0070E0"> Username: </label>
                    <label class=" form-label m-2 me-5 text-black-50"> @ComplaintReply.Username </label>
                    <label class="ms-5 form-label text-black-50"> @ComplaintReply.Created.ToLongDateString() at 
                        @ComplaintReply.Created.ToShortTimeString() </label>
                    <div class="m-1">
                        <textarea class="text-black text-wrap border-top-0 border-start-0 border-end-0" cols="100" disabled>
                            @Encoding.ASCII.GetString(ComplaintReply!.Description!)
                        </textarea>
                    </div>
                    <div>
                        <CascadingValue Value="complaint">
                            <CreateReply />
                        </CascadingValue>
                    </div>
                </div>
            </div>
        }
    </div>
</AuthorizeView>

@code {
    [CascadingParameter]
    Complaint? complaint { get; set; } = default!;

    //GridItemsProvider<ComplaintReply>? ComplaintReplyGridProvider;

    [SupplyParameterFromQuery]
    public int replyId { get; set; }

    [CascadingParameter]
    public ComplaintReply? ComplaintReply { get; set; } = default!;

    private UIHelper UIhelper = new();

    private bool ReplyClicked { get; set; } = false;

    [CascadingParameter]
    private HttpContext HttpContext { get; set; } = default!;

    protected override void OnInitialized()
    {

        if (ComplaintReply is null)
        {
            NavigationManager.NavigateTo("notfound");
        }

        //var rows = response?.AsEnumerable<ComplaintReply>().ToList<ComplaintReply>();
        //var result = GridItemsProviderResult.From<ComplaintReply>(items: rows, totalItemCount: rows.Count);
        //ComplaintReplyGridProvider = req => ValueTask.FromResult(result);
    }
}
